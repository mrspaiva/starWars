import styled from 'styled-components';

export const App = styled.div`
  .content {
    position: fixed;
    width: 100vw;
    height: 10vh;
    background-color: rgba(99, 99, 99, 0.2);
  }

  .clickLogo img{
    width: 8rem;
    display: block;
    margin: 0 auto;
    cursor: pointer;
  }
`;